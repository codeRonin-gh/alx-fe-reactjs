# ALX ReactJS Project

This repository contains my work for the ALX Frontend ReactJS introduction module.

## Tasks Completed

### 0. Create Your First React App
- Set up using Vite with React template.

### 1. Modify JSX in a Pre-built React Component
- Created `WelcomeMessage.jsx` and integrated it into the app.

### 2. Create Specific Components
- Built `Header`, `MainContent`, and `Footer` components and rendered them in `App.jsx`.

### 3. Create a User Profile Card Using Props
- Built `UserProfile.jsx` with props for name, age, and bio.

## Running the App Locally

```bash
cd alx-react-app
npm install
npm run dev
